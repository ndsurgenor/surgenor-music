{% extends 'base.twig' %}
{% block title %}Worship Songs & Resources | Surgenor Music{% endblock %}

{% block content %}

{# HERO #}
<section class="relative flex items-end justify-center bg-black/85 overflow-hidden min-h-[600px]">
    {# Video #}
    <div class="absolute inset-0 max-w-screen-3xl mx-auto left-1/2 -translate-x-1/2 w-full">
        <video autoplay muted loop playsinline poster="/assets/images/hero.jpg"
               class="w-full h-full object-cover">
            <source src="/assets/videos/hero.webm" type="video/webm">
        </video>
        <div class="absolute inset-0 bg-gradient-to-b from-charcoal/75 to-navy/85"></div>
    </div>
    {# Text & CTAs #}
    <div class="absolute z-10 text-center text-white px-6 mx-auto py-20 bottom-0">
        {# H1 #}
        <h1 class="hero-text font-hero text-4xl sm:text-5xl ![line-height:1.15] tracking-wide">
            <span class="text-secondary text-5xl sm:text-6xl">W</span>orship
            <span class="font-semibold">Songs</span> + <span class="font-semibold">Resources</span><br>
            <span class="font-light">for the local church</span>
        </h1>
        {# H2 #}
        <h2 class="hero-text text-lg md:text-xl ![line-height:1.15] mt-4 text-charcoal-500">
            from musician <span class="font-semibold">Nathan Surgenor</span>
        </h2>
        {# CTAs #}
        <div class="flex flex-wrap items-center justify-center gap-4 mt-10">
            <a href="/songs" class="btn-primary-lg">Song Catalogue</a>
            <a href="/contact" class="btn-accent-outline-lg">Contact Us</a>
        </div>
    </div>
</section>

{# FEATURED SONGS #}
{% if featured %}
<section class="max-w-6xl mx-auto px-6 py-16">
    <h2 class="text-2xl font-bold text-gray-900 mb-8">Featured Songs</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
        {% for song in featured %}
        <a href="/songs/{{ song.slug }}" class="card hover:border-primary hover:shadow-sm transition-all group">
            <h3 class="font-semibold text-gray-900 group-hover:text-primary transition-colors mb-1">
                {{ song.title }}
            </h3>
            {% if song.song_key %}
            <p class="text-sm text-gray-500">Key of {{ song.song_key }}</p>
            {% endif %}
            {% if song.tags %}
            <div class="flex flex-wrap gap-1 mt-3">
                {% for tag in song.tags %}
                <span class="tag-badge">{{ tag.name }}</span>
                {% endfor %}
            </div>
            {% endif %}
        </a>
        {% endfor %}
    </div>
    <div class="mt-8">
        <a href="/songs" class="btn-primary">View All Songs</a>
    </div>
</section>
{% endif %}

{% endblock %}
